import react from "react";
import "./Description.css";
import { ScrollContainer, ScrollPage } from "react-scroll-motion";
function Description() {
  return (
    <>
      <div>
        <h1 className="title">Arrays</h1>
        <p className="paragraph">
          According to the BBC, an array is defined as a data structure that
          holds similar, related data. With that in mind, this webpage is meant
          for you to conceptionalize the idea of an array even better by using
          an interative visaulization method.
        </p>
      </div>
    </>
  );
}

export default Description;
